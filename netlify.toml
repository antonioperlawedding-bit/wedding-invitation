[build]
  base      = "wedding-app"
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"

# Netlify Functions power the CMS and RSVP backend.
[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/api?path=:splat"
  status = 200
  force  = true

# SPA fallback — invitation and /cms routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
