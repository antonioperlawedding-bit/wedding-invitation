[build]
  base      = "wedding-app"
  command   = "npm run build"
  publish   = "wedding-app/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
